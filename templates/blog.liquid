<section class="">
    <h1 class="text-2xl mb-4">{{ meta.h1 }}</h1>
    <div class="{{ article.format }} mb-4">{{ blog.html }}</div>
    <div class="grid grid-cols-3 gap-4 mb-4">
        {% unless blog.articles == empty %}
        {% for article in blog.articles %}
            {% include 'articleCard' %}
    	{% endfor %}
        {% endunless %}
    </div>
</section>
